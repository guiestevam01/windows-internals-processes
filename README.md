This repository contains a simple study about process creation on Windows
using the Win32 API.

## Description

The example demonstrates how to create a process using the
`CreateProcess` function and how Windows handles access control
when attempting to execute a protected system file.
